import { SxProps } from '@mui/material';

export interface CTACardInterface {
  icon: string;
  iconProps?: SxProps;
  text: String;
  cardProps?: SxProps;
  buttonText: string;
  buttonProps?: SxProps;
}
